name: Order - Security Gate

on:
  push:
    paths:
      - src/Services/Order/**.cs
      - src/Services/Order/**.csproj
      - Directory.Build.props
      - .github/workflows/**

jobs:
  order-codeql:
    uses: ./.github/workflows/_codeql.yaml
    with:
      path: ./src/Services/Order