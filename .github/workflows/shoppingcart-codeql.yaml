name: Shopping Cart - Security Gate

on:
  push:
    paths:
      - src/Services/ShoppingCart/**.cs
      - src/Services/ShoppingCart/**.csproj
      - Directory.Build.props
      - .github/workflows/**

jobs:
  shoppingcart-codeql:
    uses: ./.github/workflows/_codeql.yaml
    with:
      path: ./src/Services/ShoppingCart